<table><tr><td> <em>Assignment: </em> IT202 JavaScript and CSS Challenge</td></tr>
<tr><td> <em>Student: </em> Bassil Saleh (bs92)</td></tr>
<tr><td> <em>Generated: </em> 3/23/2024 3:08:21 PM</td></tr>
<tr><td> <em>Grading Link: </em> <a rel="noreferrer noopener" href="https://learn.ethereallab.app/homework/IT202-102-S2024/it202-javascript-and-css-challenge/grade/bs92" target="_blank">Grading</a></td></tr></table>
<table><tr><td> <em>Instructions: </em> <ul><li>Reminder: Make sure you start in dev and it's up to date<ol><li><code>git checkout dev</code></li><li><code>git pull origin dev</code></li><li><code>git checkout -b M3-Challenge-HW</code></li></ol></li></ul><ol><li>Create a copy of the template given here:&nbsp;<a href="https://gist.github.com/MattToegel/77e4b66e3c73c074ea215562ebce717c">https://gist.github.com/MattToegel/77e4b66e3c73c074ea215562ebce717c</a></li><li>Implement the changes defined in the body of the code</li><ol><li>Hint: You may want to use your browser's developer tools to see the script that's pulled in, this may help with a few challenges</li></ol><li><strong>Do not</strong>&nbsp;edit anything where the comments tell you not to edit, you will lose points for not following directions</li><li>Make changes where the comments tell you (via TODO's or just above the lines that tell you not to edit below)<ol><li><strong>Hint</strong>: Just change things in the designated&nbsp;<code>&lt;style&gt;</code>&nbsp;and&nbsp;<code>&lt;script&gt;</code>&nbsp;tags</li><li><strong>Important</strong>: The function that drives one of the challenges is&nbsp;<code>updateCurrentPage(str)</code>&nbsp;which takes 1 parameter, a string of the word to display as the current page. This function is not included in the code of the page, along with a few other things, are linked via an external js file. Make sure you do not delete this line.</li></ol></li><li>Create a branch called M3-Challenge-HW if you haven't yet</li><li>Add this template to that branch (git add/git commit)</li><li>Make a pull request for this branch once you push it</li><li>You may manually deploy the HW branch to dev to get the evidence for the below prompts</li><li>Create a new file&nbsp;<strong>m3_submission.md</strong>&nbsp;file in the hw branch and fill it with the output generated from this page (be careful not to paste more than once)</li><li>Add, commit, and push the submission file</li><li>Close the pull request by merging it to dev (double-check all looks good on dev)</li><li>Manually create a new pull request from dev to prod (i.e., base: prod &lt;- compare: dev)</li><li>Complete the merge to deploy to production</li><li>Submit the direct link of the m3_submission.md from the prod branch to Canvas for this submission</li></ol><style>` and `<script>` tags
    2. **Important**: The function that drives one of the challenges is `updateCurrentPage(str)` which takes 1 parameter, a string of the word to display as the current page. This function is not included in the code of the page, along with a few other things, are linked via an external js file. Make sure you do not delete this line.  
5. Create a branch called M3-Challenge-HW if you haven't yet
6. Add this template to that branch (git add/git commit)
7. Make a pull request for this branch once you push it
8. You may manually deploy the HW branch to dev to get the evidence for the below prompts
9. Create a new file **m3_submission.md** file in the hw branch and fill it with the output generated from this page (be careful not to paste more than once)
10. Add, commit, and push the submission file
11. Close the pull request by merging it to dev (double-check all looks good on dev)
12. Manually create a new pull request from dev to prod (i.e., base: prod <- compare: dev)
13. Complete the merge to deploy to production
14. Submit the direct link of the m3_submission.md from the prod branch to Canvas for this submission
</style></td></tr></table>
<table><tr><td> <em>Deliverable 1: </em> Completed Challenge Screenshots </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> 5 Screenshots based on the checklist items for this task</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://firebasestorage.googleapis.com/v0/b/learn-e1de9.appspot.com/o/assignments%2Fbs92%2F2024-03-23T16.41.56HW3_Checklist.png.webp?alt=media&token=3b9d5094-3912-4565-b474-070859057008"/></td></tr>
<tr><td> <em>Caption:</em> <p>Screenshot of the primary page with checklist items completed.<br></p>
</td></tr>
<tr><td><img width="768px" src="https://firebasestorage.googleapis.com/v0/b/learn-e1de9.appspot.com/o/assignments%2Fbs92%2F2024-03-23T16.43.15HW3_Login.png.webp?alt=media&token=33dfc33c-c6da-4ed0-bb47-1f570d57e1c3"/></td></tr>
<tr><td> <em>Caption:</em> <p>Screenshot of the webpage after clicking the Login link in the navigation bar.<br></p>
</td></tr>
<tr><td><img width="768px" src="https://firebasestorage.googleapis.com/v0/b/learn-e1de9.appspot.com/o/assignments%2Fbs92%2F2024-03-23T16.44.18HW3_Register.png.webp?alt=media&token=55292366-f34b-4a79-857c-7aff298a807c"/></td></tr>
<tr><td> <em>Caption:</em> <p>Screenshot of the webpage after clicking the Register link in the navigation bar.<br></p>
</td></tr>
<tr><td><img width="768px" src="https://firebasestorage.googleapis.com/v0/b/learn-e1de9.appspot.com/o/assignments%2Fbs92%2F2024-03-23T16.45.28HW3_Profile.png.webp?alt=media&token=cc8d290b-2de8-46db-9d3e-2f4a6ee73bda"/></td></tr>
<tr><td> <em>Caption:</em> <p>Screenshot of the webpage after clicking the Profile link in the navigation bar.<br></p>
</td></tr>
<tr><td><img width="768px" src="https://firebasestorage.googleapis.com/v0/b/learn-e1de9.appspot.com/o/assignments%2Fbs92%2F2024-03-23T16.46.20HW3_Logout.png.webp?alt=media&token=1c0d4805-0398-40ad-8ab2-9af8695c0708"/></td></tr>
<tr><td> <em>Caption:</em> <p>Screenshot of the webpage after clicking the Logout link in the navigation bar.<br></p>
</td></tr>
</table></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 2: </em> Explain Solutions (Grader use this section to determine completion of each challenge) </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Briefly explain how you made the navigation horizontal</td></tr>
<tr><td> <em>Response:</em> <p>I selected the list items enclosed in &lt;nav&gt; by using the &quot;nav li&quot;<br>selector. Then I set the &quot;display: inline&quot; rule for that selector so that<br>each navigation bar link would be displayed as an inline element. In other<br>words, the rule displays each navigation link one after the other (only using<br>as much width as necessary to spell out each link&#39;s label) as opposed<br>to displaying each link on a new line.<br></p><br></td></tr>
<tr><td> <em>Sub-Task 2: </em> Briefly explain how you remove the navigation list item markers</td></tr>
<tr><td> <em>Response:</em> <p>I used the &quot;nav ul&quot; selector to select unordered lists enclosed in &lt;nav&gt;.<br>Then I gave it a CSS rule of &quot;list-style-type: none&quot;, which prevents any<br>item markers from showing next to each navigation bar link.<br></p><br></td></tr>
<tr><td> <em>Sub-Task 3: </em> Briefly explain how you gave the navigation a background</td></tr>
<tr><td> <em>Response:</em> <p>I used the &quot;nav ul&quot; selector to select unordered lists enclosed in &lt;nav&gt;.<br>Then I added to it a CSS rule &quot;background-color: var(--3);&quot;, where the variable<br>&quot;--3&quot; is the hexadecimal value&nbsp;#bc9ec1.<br></p><br></td></tr>
<tr><td> <em>Sub-Task 4: </em> Briefly explain how you made the links (or their surrounding area) change color on mouseover/hover</td></tr>
<tr><td> <em>Response:</em> <p>I used the &quot;nav li a:hover&quot; selector to select &lt;a&gt; tags inside &lt;li&gt;<br>which are inside &lt;nav&gt;. The &quot;:hover&quot; selector means that the CSS rule is<br>applied only if the mouse is currently over that element. Then I added<br>to it some CSS rules:<div><ul><li>&quot;background-color: var(--1);&quot; changes the background color using the value<br>of variable &quot;--1&quot;, or #f5f4e3.</li><li>&quot;color: var(--4);&quot; changes the color of the navigation bar<br>link&#39;s text using the value of variable &quot;--4&quot;, or #596475.</li></ul></div><br></p><br></td></tr>
<tr><td> <em>Sub-Task 5: </em> Briefly explain how you changed the challenge list bullet points to checkmarks (✓)</td></tr>
<tr><td> <em>Response:</em> <p>I used the &quot;h3+ul li::before&quot; selector to select the challenge list. The &quot;h3+ul&quot;<br>selector gets the first unordered list right after the &lt;h3&gt; header which reads<br>&quot;Challenges&quot;. The &quot;li::before&quot; selector adds styling/content before the contents nested inside the &lt;li&gt;<br>tags are rendered.<div><br></div><div>For this combination of selectors, I added to it the CSS<br>rule &#39;content:&quot;\2713&quot;&#39;, where \ represents an escape character and 2713 is the ASCII<br>value of the checkmark character in hexadecimal. The &quot;padding-bottom: 4px&quot; is not necessary<br>for this challenge subtask; I just added it since I wanted more spacing<br>between the list of challenge tasks.</div><br></p><br></td></tr>
<tr><td> <em>Sub-Task 6: </em> Briefly explain how you made the first character of the h1 tags and anchor tags uppercased</td></tr>
<tr><td> <em>Response:</em> <p>I used the &quot;a, h1&quot; selector to select all &lt;a&gt; elements and all<br>&lt;h1&gt; elements in the HTML document. Then I added to it a CSS<br>rule &quot;text-transform: capitalize&quot; which capitalizes the 1st letter of every word inside the<br>&lt;a&gt; and &lt;h1&gt; tags.<br></p><br></td></tr>
<tr><td> <em>Sub-Task 7: </em> Briefly explain/describe your custom styling of your choice</td></tr>
<tr><td> <em>Response:</em> <div>"nav li a" selector (for navigation bar links):<br></div><div><ul><li>Font changed to Segoe UI (bold<br>text).</li><li>10px padding between links.</li><li>Center aligned text.</li><li>12px border radius around each link.</li><li>Color of font<br>changed to var(--5), or #1f2232.</li></ul></div><div>"body" selector (every other visible element in the HTML<br>document):</div><div><ul><li>Font in rest of document changed to Verdana.</li><li>Color of font changed to var(--5),<br>or #1f2232.</li></ul><div>"nav ul" selector (the navigation bar):</div></div><div><ul><li>10px 50px 10px 10px margin.</li><li>10px padding on<br>the top and bottom.</li><li>12px border radius.</li></ul><div>"h3+ul li::before" selector (1st unordered list after "Challenge"<br>&lt;h3&gt; header):</div></div><div><ul><li>4px padding to the right (for extra space between the checkmark and<br>the text).</li></ul><div>"h1,h2,h3" selector (headings)</div></div><div><ul><li>10px padding.</li></ul></div><br></td></tr>
<tr><td> <em>Sub-Task 8: </em> Briefly explain how the styling for the challenge list doesn't impact the navigation list</td></tr>
<tr><td> <em>Response:</em> <p>The navigation list&#39;s styling does not affect the challenge list because the navigation<br>list&#39;s style rules begin with &quot;nav&quot;. Style rules that begin with &quot;nav&quot; are<br>restricted only to &lt;nav&gt; elements and other tags enclosed in &lt;nav&gt;; since the<br>challenge list is not enclosed in &lt;nav&gt;, the navigation bar&#39;s style rules don&#39;t<br>apply to it.<br></p><br></td></tr>
<tr><td> <em>Sub-Task 9: </em> Briefly explain how you updated the content of the h1 tag with the link text</td></tr>
<tr><td> <em>Response:</em> <p>I added an event listener to check for a click event using the<br>following code in the &lt;script&gt; tag:<div><pre>window.addEventListener("click", function() {<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;<br>getCurrentSelection();<br>&nbsp; &nbsp; &nbsp; &nbsp; });</pre></div><div>Here is the code in getCurrentSelection():</div><div><pre>function getCurrentSelection() {<br>&nbsp; &nbsp;<br>&nbsp; &nbsp; &nbsp; &nbsp; section_name = '';<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; setTimeout(()<br>=&gt; {<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; //added this delay here<br>as some solutions may require it to update properly (i.e., click code may<br>complete before the navigation changes)<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; //TODO:<br>add code for processing the current selection&nbsp;<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;<br>&nbsp; section_name = window.location.hash;<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; if(section_name !=<br>'') {<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; section_name =<br>section_name.slice(1,section_name.length);<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; updateCurrentPage(section_name);<br>&nbsp; &nbsp; &nbsp;<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; }<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;<br>//Note: likely you'll want to call updateCurrentPage towards the end<br>&nbsp; &nbsp; &nbsp; &nbsp;<br>&nbsp; &nbsp; }, 100);<br>&nbsp; &nbsp; &nbsp; &nbsp; }</pre></div><div>I added a section_name variable before<br>the setTimeout() function call. Inside the setTimeout() code, I set section_name equal to<br>the part of the current url with the # symbol (window.location.hash). I then<br>check if section_name is a non-empty string (if the user happened to click<br>somewhere else besides the navigation bar, then the &quot;Start&quot; &lt;h1&gt; header won&#39;t be<br>overwritten with an empty string). If section_name is non-empty, then I get a<br>substring of it (removing the # character) and call updateCurrentPage(section_name) to update the<br>&lt;h1&gt; header (and the &lt;title&gt; header) with the name of the clicked navigation<br>link.</div><div><br></div><div>The reason I put code for updating the &lt;h1&gt; tag with the current<br>page name inside a setTimeout() function call is because the code being executed<br>upon detecting a mouse click executes faster than the web browser can update<br>the URL with the hash (#) segment. By the time the setTimeout() function<br>call has waited 100 milliseconds, the URL will have been updated with the<br>correct webpage section.</div><br></p><br></td></tr>
<tr><td> <em>Sub-Task 10: </em> Briefly explain how you updated the content of the title tag with the link text</td></tr>
<tr><td> <em>Response:</em> <p>I added an event listener to check for a click event using the<br>following code in the &lt;script&gt; tag:<div><pre>window.addEventListener("click", function() {<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;<br>getCurrentSelection();<br>&nbsp; &nbsp; &nbsp; &nbsp; });</pre></div><div>Here is the code in getCurrentSelection():</div><div><pre>function getCurrentSelection() {<br>&nbsp; &nbsp;<br>&nbsp; &nbsp; &nbsp; &nbsp; section_name = '';<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; setTimeout(()<br>=&gt; {<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; //added this delay here<br>as some solutions may require it to update properly (i.e., click code may<br>complete before the navigation changes)<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; //TODO:<br>add code for processing the current selection&nbsp;<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;<br>&nbsp; section_name = window.location.hash;<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; if(section_name !=<br>'') {<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; section_name =<br>section_name.slice(1,section_name.length);<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; updateCurrentPage(section_name);<br>&nbsp; &nbsp; &nbsp;<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; }<br>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;<br>//Note: likely you'll want to call updateCurrentPage towards the end<br>&nbsp; &nbsp; &nbsp; &nbsp;<br>&nbsp; &nbsp; }, 100);<br>&nbsp; &nbsp; &nbsp; &nbsp; }</pre></div><div>I added a section_name variable before<br>the setTimeout() function call. Inside the setTimeout() code, I set section_name equal to<br>the part of the current url with the # symbol (window.location.hash). I then<br>check if section_name is a non-empty string (if the user happened to click<br>somewhere else besides the navigation bar, then the &quot;Start&quot; &lt;title&gt; header won&#39;t be<br>overwritten with an empty string). If section_name is non-empty, then I get a<br>substring of it (removing the # character) and call updateCurrentPage(section_name) to update the<br>&lt;title&gt; header (and the &lt;h1&gt; header) with the name of the clicked navigation<br>link.</div><div><br></div><div>The reason I put code for updating the &lt;title&gt; tag with the current<br>page name inside a setTimeout() function call is because the code being executed<br>upon detecting a mouse click executes faster than the web browser can update<br>the URL with the hash (#) segment. By the time the setTimeout() function<br>call has waited 100 milliseconds, the URL will have been updated with the<br>correct webpage section.</div><br></p><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 3: </em> Misc </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707834-bf5a5b13-ec36-4597-9741-aa830c195be2.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Comment briefly talking about what you learned and/or any difficulties you encountered and how you resolved them (or attempted to)</td></tr>
<tr><td> <em>Response:</em> <p>When attempting to get the webpage to change the &lt;h1&gt; and &lt;title&gt; tags<br>every time the user clicked on a navigation bar link, I initially had<br>trouble figuring out how to properly update these tags. My initial attempt at<br>the problem involved reading (window.location.hash) right when the web browser detects a click.<br>The reason that didn&#39;t work was because the web browser runs code in<br>window.addEventListener(&quot;click&quot;, function() {...}) faster than the URL can update. So I moved the<br>code for updating the &lt;h1&gt; and &lt;title&gt; headers into a setTimeout() call, which<br>is inside getCurrentSelection().<br></p><br></td></tr>
<tr><td> <em>Sub-Task 2: </em> Add a link to your pull request (hw branch to dev only)</td></tr>
<tr><td> <a rel="noreferrer noopener" target="_blank" href="https://github.com/Bassil-Saleh/bs92-IT202-102/pull/20">https://github.com/Bassil-Saleh/bs92-IT202-102/pull/20</a> </td></tr>
<tr><td> <em>Sub-Task 3: </em> Add a link to your solution html file from prod (again you can assume the url at this point)</td></tr>
<tr><td>Not provided</td></tr>
</table></td></tr>
<table><tr><td><em>Grading Link: </em><a rel="noreferrer noopener" href="https://learn.ethereallab.app/homework/IT202-102-S2024/it202-javascript-and-css-challenge/grade/bs92" target="_blank">Grading</a></td></tr></table>