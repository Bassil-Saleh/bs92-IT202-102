<table><tr><td> <em>Assignment: </em> IT202 M2 PHP-HW</td></tr>
<tr><td> <em>Student: </em> Bassil Saleh (bs92)</td></tr>
<tr><td> <em>Generated: </em> 2/16/2024 9:47:31 PM</td></tr>
<tr><td> <em>Grading Link: </em> <a rel="noreferrer noopener" href="https://learn.ethereallab.app/homework/IT202-102-S2024/it202-m2-php-hw/grade/bs92" target="_blank">Grading</a></td></tr></table>
<table><tr><td> <em>Instructions: </em> <p>Make sure you have the dev/prod branches created before starting this assignment.</p><p><strong>Template Files</strong>&nbsp;You can find all 3 template files in this gist:&nbsp;<a href="https://gist.github.com/MattToegel/48b48377eaa1937c886b7840c449750a">https://gist.github.com/MattToegel/48b48377eaa1937c886b7840c449750a</a><br></p><p>Setup steps:</p><ol><li><code>git checkout dev</code></li><li><code>git pull origin dev</code></li><li><code>git checkout -b M2-PHP-HW</code></li></ol><p>You'll have 3 problems to save for this assignment.</p><p>Each problem you're given a template&nbsp;<strong>Do not edit anything in the template except where the comments tell you to</strong>.</p><p>The templates are done in such a way to make it easier to capture the output in a screenshot (if it's still not able to fit well, you can zoom out in your browser).</p><p>You'll copy each template into their own separate .php files, immediately git add, git commit these files (you can do it together) so we can capture the difference/changes between the templates and your additions. This part is required for full credit.</p><p>HW steps:</p><ol><li>Open VS Code at the root of your repository folder (you should see the Procfile and all from the Heroku setup)</li><li>In VS Code create a new folder/directory in public_html called M2</li><li>Create 3 new files in this new M2 folder (problem1.php, problem2.php, problem3.php)</li><li>Paste each template into their respective files</li><li><code>git add .</code></li><li><code>git commit -m "adding template baselines</code></li><li>Do the related work (you may do steps 8 and 9 as often as needed or you can do it all at once at the end)</li><li><code>git add .</code></li><li><code>git commit -m "completed hw"</code></li><li>When you're done push the branch<ol><li><code>git push origin M2-PHP-HW</code></li></ol></li><li>Go to heroku dev, and manually deploy the M2-PHP-HW branch</li><li>After it deploys go to&nbsp;<a href="https://ucid-dev.herokuapp.com/M2/problem1.php">https://ucid-dev.herokuapp.com/M2/problem1.php</a>&nbsp;(replace ucid with your ucid if you followed the setup instructions)</li><li>Update the URL to check that each problem file displays properly</li><li>Create the Pull Request with&nbsp;<strong>dev</strong>&nbsp;as base and&nbsp;<strong>M2-PHP-HW</strong>&nbsp;as compare</li><li>Create a new file in the M2 folder in VS Code called m2_submission.md</li><li>Fill out the below deliverable items, save the submission, and copy to markdown<ol><li>For this assignment you may get screenshots from your heroku dev instance, you do not need to move it to prod then come back and update it</li></ol></li><li>Paste the markdown into the m2_submission.md</li><li>add/commit/push the md file<ol><li><code>git add m2_submission.md</code></li><li><code>git commit -m "adding submission file"</code></li><li><code>git push origin M2-PHP-HW</code></li></ol></li><li>Merge the pull request from step 14</li><li>Create a new pull request with prod as base and dev as compare</li><li>Immediately create/merge/confirm, this is just to deploy it to prod and you don't need to adjust anything during this step</li><li>On your local machine sync the changes<ol><li><code>git checkout dev</code></li><li><code>git pull origin dev</code></li></ol></li><li>Submit the link to the m2_submission.md file from the prod branch to Canvas</li></ol><p><br></p></td></tr></table>
<table><tr><td> <em>Deliverable 1: </em> Problem 1 - Only output Odd values of the Array under "Odds output" </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> 2 Screenshots: Clearly screenshot the output of Problem 1 and show the edited code</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://i.imgur.com/ocTfGRR.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>My code for printing odd numbers in problem1.php. Comments include my name, UCID<br>and date the code was written.<br></p>
</td></tr>
<tr><td><img width="768px" src="https://i.imgur.com/rAvuS1a.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>Output of my code for problem1.php<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Describe how you solved the problem</td></tr>
<tr><td> <em>Response:</em> <div>Dividing an even number by 2 always yields a remainder of zero.&nbsp;</div><div>This is<br>because even numbers can always be written as a multiple of 2.&nbsp;</div><div>Hence a<br>formula for getting even numbers can be constructed as:</div><div>a = 2n, where n<br>is any integer.</div><div><br></div><div>Adding 1 to this formula yields another formula for getting odd<br>numbers:</div><div>b = 2n + 1, where n is any integer.</div><div><br></div><div>Knowing that even numbers<br>can be constructed as multiples of 2 means that:</div><div>2n % 2 = 0</div><div><br></div><div>We<br>can use this result (along with a few algebraic properties of the modulus<br>operator)</div><div>to see that dividing an odd number by 2 always yields a remainder<br>of one:</div><div>(2n + 1) % 2 = (2n % 2) + (1 %<br>2) = 0 + 1</div><div>(2n + 1) % 2 = 1</div><div><br></div><div>This is why<br>my code checks for a remainder of 1 when performing modulus division by<br>2 on each element of the passed array before echoing it.</div><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 2: </em> Problem 2 - Only output the sum/total of the array values by assigning it to the $total variable (the number must end in 2 decimal places, if it ends in 1 it must have a 0 at the end) </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> 2 Screenshots: Clearly screenshot the output of Problem 2 showing the data and show the edited code</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://i.imgur.com/L5tvvna.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>My code for problem2.php, including comments with my name, UCID and the date<br>the code was written.<br></p>
</td></tr>
<tr><td><img width="768px" src="https://i.imgur.com/CAvzBuR.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>Screenshot of the output of my code for problem2.php<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Describe how you solved the problem</td></tr>
<tr><td> <em>Response:</em> <p>For the totaling logic, I used a foreach() loop to access each individual<br>element in the passed array.<div>In each loop iteration, I used the += operator<br>to add each accessed element to the current value of total.</div><div><br></div><div>For the rounding<br>logic, I passed $total into the round() function (specifying to round to two<br>decimal places) and assigned the result back into $total.</div><div><br></div><div>When testing my code, I<br>noticed that if the rounded number ends in 1 decimal place (i.e. 0.10),<br>then echoing it leaves out the trailing zero.</div><div>To prevent this, I used number_format()<br>to save the value of $total (after it was rounded using the round()<br>function) as a formatted string</div><div>that always includes two decimal places. However, this means<br>that $total is replaced by a string; I don&#39;t know if there is<br>a way to&nbsp;<br></div><div>print trailing zeroes while preserving the data type of $total as<br>a float.</div><br></p><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 3: </em> Problem 3 - Output the given values as positive under the "Positive Output" message (the data otherwise shouldn't change) </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> 2 Screenshots: Clearly screenshot the output of Problem 3 showing the data and show the code</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://i.imgur.com/pj4cIHk.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>Output of my code for problem3.php<br></p>
</td></tr>
<tr><td><img width="768px" src="https://i.imgur.com/y0BHdtH.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>My code for problem3.php, with comments including my name, UCID, and the date<br>the code was written.<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Describe how you solved the problem</td></tr>
<tr><td> <em>Response:</em> <p>For the positive conversion, I used two separate conditional code blocks; one for<br>handling string data types and another for handing non-string types.<br><div>I check to see<br>if the element is a string first, since short circuit evaluation will prevent<br>PHP from attempting to extract a character from a non-string element.</div><div>If the element<br>happens to be a string and its first character is &#39;-&#39;, I then<br>use str_replace() to replace the occurrence of &#39;-&#39; with an empty substring.</div><div><br></div><div>If the<br>data type is not a string, then I just check if the element<br>is less than zero. If so, then I multiply it by -1 and<br>reassign the result to the element.<br></div><div>By the point this line of code runs,<br>the element should be a numeric data type (int, float, etc.), so multiplying<br>it by the integer -1 should not&nbsp;<br></div><div>cause a narrowing conversion to its data<br>type. In other words, multiplying a float by the integer -1 yields a<br>float-type result, while&nbsp;<br></div><div>integers multiplied by -1 yields an integer-type result, so at this<br>point I don&#39;t need to do any explicit type conversion/casting.</div><br></p><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 4: </em> Misc Items </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707834-bf5a5b13-ec36-4597-9741-aa830c195be2.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Add the prod URL for problem1.php (remember you can assume this based on how the domain gets built (i.e., ucid-prod.herokuapp.com/...)</td></tr>
<tr><td>Not provided</td></tr>
<tr><td> <em>Sub-Task 2: </em> Add the prod URL for problem2.php (remember you can assume this based on how the domain gets built (i.e., ucid-prod.herokuapp.com/...)</td></tr>
<tr><td>Not provided</td></tr>
<tr><td> <em>Sub-Task 3: </em> Add the prod URL for problem3.php (remember you can assume this based on how the domain gets built (i.e., ucid-prod.herokuapp.com/...)</td></tr>
<tr><td>Not provided</td></tr>
<tr><td> <em>Sub-Task 4: </em> Pull Request URL for M2-PHP-HW to dev</td></tr>
<tr><td> <a rel="noreferrer noopener" target="_blank" href="https://github.com/Bassil-Saleh/bs92-IT202-102/pull/5">https://github.com/Bassil-Saleh/bs92-IT202-102/pull/5</a> </td></tr>
<tr><td> <em>Sub-Task 5: </em> Talk about what you learned, any issues you had, how you resolve them</td></tr>
<tr><td> <em>Response:</em> <p>New things I learned about PHP:<div><ul><li>PHP supports short circuit evaluation for Boolean expressions.<br>I saw this in my code for problem2.php; having the first Boolean expression<br>evaluating to false seems to have prevented PHP from executing the second Boolean<br>expression (extracting the first letter from a string).</li><li>I was surprised to learn that<br>PHP can implicitly convert strings (which only contain numerical data) into numeric data<br>types if an operation calls for it (i.e. &quot;-4&quot; * 2). Although I<br>did not use this for problem3.php, I discovered it while testing out my<br>preliminary code.</li></ul></div><br></p><br></td></tr>
</table></td></tr>
<table><tr><td><em>Grading Link: </em><a rel="noreferrer noopener" href="https://learn.ethereallab.app/homework/IT202-102-S2024/it202-m2-php-hw/grade/bs92" target="_blank">Grading</a></td></tr></table>